# Database

Database layer for SQLAlchemy models, session management, and repositories.

PostgreSQL stores metadata, audit history, findings, risk scores, report references, and storage paths. It must not store raw uploaded PDFs.

